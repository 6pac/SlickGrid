/**
 * Compatibility export. The implementation lives in `slick.core.ts` so IIFE
 * consumers get it from the core script without loading another file first.
 */
export { DockingController } from './slick.core.js';
